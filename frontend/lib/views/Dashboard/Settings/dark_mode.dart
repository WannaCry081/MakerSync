import "package:flutter/material.dart";

class DarkModeView extends StatelessWidget {
  const DarkModeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("DarkModeView"));
  }
}