import "package:flutter/material.dart";

class MSDialogWidget extends StatelessWidget {
  const MSDialogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}